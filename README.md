# City-navigation-system
The City Navigation System is a C-based project that uses Dijkstra's Algorithm to find the shortest path between cities. Cities are represented as nodes and roads as weighted edges in a graph. The system calculates the minimum distance and optimal route, demonstrating the practical application of Data Structures and Graph Algorithms.
